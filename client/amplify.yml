version: 1
frontend:
    phases:
        preBuild:
            commands:
                - cd kendavis/client
                - 'npm install'
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: build/
        files:
            - '**/*'
    cache:
        paths: []
